# 更新日志

## 1.0.17.0 — 2026-09-13（内部预发布）

### 问题修复
- 工作簿重命名接入 OneDrive 本机同步路径解析，避免将云端网址作为本地目录；确认新文件保存完成并切换成功后再删除旧文件，重命名后刷新导航栏。
- 重命名拒绝覆盖同名文件、只读文件及无法确认的本地路径；保存取消或失败时保留旧文件，旧文件清理失败时明确提示新旧路径。

### 新增功能
- 工作表列表支持拖动排序，插入线指示放置位置，靠近边缘自动滚动；按目标表前后调整真实 Sheet 顺序，名称搜索和隐藏表筛选下也可使用。
- 普通隐藏表可直接移动并保持隐藏，移动后保留活动表、刷新同一工作簿的导航栏；深度隐藏表保持只读，编辑中和结构保护时禁止移动。

## 1.0.16.0 — 2026-09-13（内部预发布）

### 体验优化
- 版本号与工作表可见/隐藏数量合并为底部一行，移除独立底栏；功能说明保留在右键菜单。
- 发现新版时在当前版本号后显示红点，点击版本号或红点查看更新，悬停可见新版本号。

## 1.0.15.0 — 2026-09-12（内部预发布）

### 新增功能
- 导航栏底部显示当前版本与“功能说明”，可离线查看各项功能的操作方式和适用限制。
- 启动后延迟后台检查更新，此后每六小时检查一次；多个窗格共享检查结果，发现新版仅在底部提示。
- 点击新版提示可查看当前版本之后的更新内容，确认“下载并安装”后才启动下载。

### 体验优化
- 更新说明直接来自发布日志，随发布清单单独签名，GitHub 与 OneView 内容一致，并保留旧客户端更新兼容性。
- 自动检查失败不弹窗，不抢焦点；下载期间防止多个窗格重复下载安装包。

## 1.0.14.0 — 2026-09-12（内部预发布）

- “复制文件”改为保存当前修改后复制原文件的实际路径，不再生成临时副本；OneDrive 云端地址通过本机同步映射解析，无法确定本地路径或保存未完成时明确提示。
- 原文件被占用或暂时无法读取时仍复制实际路径，并提示文件恢复可访问后再粘贴。

## 1.0.13.0 — 2026-09-12（内部预发布）

- 工作簿关闭按钮前增加“复制文件”，将包含当前编辑内容的同名临时副本放入文件剪贴板，方便粘贴到支持文件粘贴的聊天或邮件客户端；右键菜单也提供入口，新工作簿先保存确认名称和格式。

- 加宽工作簿重命名输入窗口，支持拉伸窗口查看长文件名，保留回车确认和 Esc 取消。
- 工作表列表刷新时保留原顶部工作表，取消隐藏后不再跳回顶部；激活其他表时只在需要时滚动到目标，切换工作簿不会沿用旧工作簿的位置。

## 1.0.12.0 — 2026-09-12（内部预发布）

- 工作簿重命名固定保留原扩展名和文件格式，名称中的日期、小数点或版本号不再被误认为扩展名；输入末尾已带原扩展名时不重复追加。
- 工作簿保存或重命名成功后自动刷新导航栏；Excel 暂时忙碌时延迟重试，恢复可操作后更新工作簿名称。

## 1.0.11.0 — 2026-09-11（内部预发布）

- 保存并关闭 Excel/WPS 的提醒窗口标题改为软件名“Excel Navigator”。
- 安装器等待 MSI 的实际结果，成功后提示安装完成及重新打开 Excel；取消和失败不显示成功提示，需要重启时单独说明。

## 1.0.10.0 — 2026-09-11（内部预发布）

- 确认更新下载后，安装包自动保存到系统临时目录下的 `ExcelNavigatorPane\Updates`，校验通过后自动打开，省去选择保存位置和手动查找安装包的步骤。
- 安装器检测到 Excel/WPS 仍在运行时，提示先保存工作并关闭，再点击“重试”继续；允许取消，不自动关闭应用。
- 支持 GitHub 与 OneView 两个更新渠道，由 `installer/UpdateChannels.json` 配置地址、打包参数 `-UpdateChannel` 选择；各渠道安装包继续从原渠道更新。GitHub 使用公开 Release 订阅源发现版本，安装包和签名清单从 Release 下载；OneView 使用对象存储。

## 1.0.9.0 — 2026-09-11（内部预发布）

- 汇总 1.0.1～1.0.8 本地测试改动：EXE/MSI 安装与签名清单更新、32/64 位宿主注册、WPS 窗格复用和工作簿切换、单表取消隐藏后跳转、Sheet 标签色、搜索清除及工作簿区域右键菜单。
- 更新请求使用 TLS 1.2；本次发布启用 RustFS 的 latest.xml 更新入口。1.0.4～1.0.8 测试版可检查并下载新版；更早版本可能需要手工下载 EXE。
- 安装器支持确认后备份迁移开发版注册，并保留诊断日志。用户反馈 WPS 切换及重复窗格初测通过，完整真实升级/回滚/卸载验收尚未完成，因此标记为预发布。

## 1.0.8.0（本地试用包，未发布）

- 工作表搜索框有内容时显示清除按钮，点击后清空关键词并保留搜索焦点及隐藏表筛选设置。
- 工作簿列表空白、标题区和边缘支持右键通用菜单；右键具体工作簿增加保存、重命名和关闭，仅操作点中的工作簿，不因右键而激活它。
- 保留三个点入口，与右键菜单共用排序、刷新、检查更新和关于等命令及状态；支持菜单键和 Shift+F10。

## 1.0.7.0（本地试用包，未发布）

- 根据安装器日志确认冲突来自项目 bin/Debug 开发版注册；安装器对 bin/Debug 或 bin/Release 的本插件本地注册提供显式确认迁移。
- 用户确认后先完整备份注册值、类型和子键到 HKCU/Software/ExcelNavigatorPane/RegistrationBackups，再移除该开发版注册继续安装；不删除项目文件，其他冲突仍拦截。
- 迁移前重新确认 Excel/WPS 已关闭及 Manifest 未变化；--check-only 不迁移。备份路径写入诊断日志，后续取消/安装失败时备份仍保留。

## 1.0.6.0（本地试用包，未发布）

- 安装器区分同一份本地安装与真正冲突的用户级注册，不再将所有 HKCU Manifest 一律判为旧版；冲突仍阻止安装，不自动删除注册。
- 每次启动保存版本、程序路径及检查时的 HKCU/HKLM Manifest 到本地 Installer/logs；错误提示包含具体冲突位置和日志路径，增加只读 --check-only 模式。
- 用户反复报错时实时 HKCU 检查及旧版独立进程检查均通过，启动瞬间的具体读值仍未捕获；此版改善判断和证据，不声称该现象已完成实机验证。

## 1.0.5.0（本地试用包，未发布）

- 导航栏工作表名称前显示原工作表标签色，保留选中行标记；白色/浅色用细边框区分，无标签色时不显示色条。
- 列表刷新时同步标签颜色，兼容无色与黑色的区别；隐藏工作表同样显示颜色，宿主不支持读取颜色时保持原导航功能。

## 1.0.4.0（本地试用包，未发布）

- 检查更新和下载安装包使用请求级 TLS 1.2，避免旧版 .NET 宿主默认 TLS 导致安全连接失败；不修改 Excel/WPS 的全局 TLS 设置。
- 更新入口返回 404 时明确提示更新清单尚未提供，区别于网络故障。本地试用包不自动发布线上清单。
- 用户反馈 1.0.3.0 的 WPS 工作簿切换及重复导航栏问题初测已解决。

## 1.0.3.0（本地试用包，未发布）

- 修复 64 位 Excel 与 32 位 WPS 共存时，安装包只注册 64 位加载项的问题。EXE 按 Windows 位数选包，64 位包将同一份 AnyCPU 插件注册到两个注册表视图。
- 32 位 MSI 限用于 32 位 Windows，避免在 64 位系统漏注册宿主；双位数注册均由 MSI 管理升级和卸载。
- 保留 1.0.2.0 的导航逻辑及用户已验证的单表取消隐藏自动跳转。WPS 切换工作簿与重复窗格仍需安装本包后实测，尚不作为已验证修复。

## 1.0.2.0（本地试用包，未发布）

- 针对已能加载插件的 WPS 表格环境，工作簿切换改用宿主 COM 接口，不再误用 Excel 专用窗口类校验；编辑或忙碌时拒绝切换。
- WPS 同一主窗口内的工作簿复用导航栏，按活动工作簿更新内容，按实际窗口集合清理关闭后的窗格；创建过程防止激活事件重入。Excel 保持每窗口独立导航栏。
- 在列表中取消单张普通隐藏表的隐藏后，立即切换到该表；Excel/WPS 共用此行为。批量取消隐藏不自动切表，VeryHidden/结构保护限制不变。
- 更新安装器同时检查 Excel 和 WPS 表格进程，防止覆盖正在使用的插件。
- DLL 文件版本同步至 1.0.2.0，打包时强制与 MSI 版本一致，避免升级时因文件版本未增长而跳过替换。
- 离线宿主代理回归、窗口分组及生命周期、取消隐藏激活顺序和打包检查通过。真实 WPS/Excel 点击回归 NOT_RUN；WPS 公式编辑导航和仅有 WPS 的电脑安装尚未适配。

## 1.0.1.0（本地试用包，未发布）

- 单个 EXE 内置 x86/x64 MSI，按 Excel 位数安装到 Program Files，使用本地 VSTO 注册，不再依赖 ClickOnce 在线安装的发布者信任。
- 安装需要管理员授权，不导入根证书、不关闭 Excel；运行中的 Excel 或旧 HKCU 注册会阻止安装。旧 ClickOnce/开发版需先手工卸载或取消注册一次。
- “检查更新”验证签名的 latest.xml，按需下载并校验新版 EXE，保存工作并关闭 Excel 后由用户运行升级；关闭 ClickOnce 启动更新。
- MSI 使用固定升级家族、阻止降级并在事务内替换旧版；版本格式改为主.次.构建.0。
- RustFS 发布保留旧 ClickOnce 入口和所有历史文件，新版产物先上传校验，latest.xml 最后更新。
- 打包跳过开发版加载项注册，避免改变当前 Excel 环境。两种位数 MSI 表/负载、EXE 解包、签名更新解析和离线发布保护检查通过；真实安装、Excel 加载、升级/回滚/卸载为 NOT_RUN。

## 1.0.0.2 — 2026-09-11（内部试用版）

### 新增与调整

- 工作簿区默认显示五行，数量增加时自动扩展；支持拖动分隔线调整及双击恢复自动高度。
- 新增隐藏工作表列表筛选，区分“仅看可见表 / 查看全部表”和“取消隐藏 / 恢复隐藏”，补充动态悬停提示。
- 统一工具栏与列表操作图标，完善悬停、聚焦和状态反馈。
- 增加单文件 EXE 安装包、完整 ClickOnce 发布 ZIP、版本显示及“检查更新”；支持发布时配置固定 HTTPS 更新源。
- 调整工作簿窗口重新定位与最小化恢复逻辑，处理窗口状态变化后的导航问题。

### 验证与限制

- 本地构建、打包解包校验及相关独立检查已通过；具体范围见 README。
- 休眠唤醒场景的真实 Excel 回归、首次安装及旧版到新版升级验收尚未完成。
- 首次接入固定在线更新源，RustFS 真实上传、覆盖、签名及匿名下载、权限拒绝检查通过。
- 使用内部自签名证书，首次安装可能提示发布者信任；旧离线安装用户需运行本版 EXE 接入在线更新源。
