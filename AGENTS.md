# 项目约定

## 发布

用户要求后续每次发布都完成：Git 提交并推送 GitHub、记录更新日志、创建 GitHub Release、上传发布到 RustFS。用户要求“发布”时，按 README 的“发布流程”完成全套操作，不再逐项重复确认；普通修改不自动触发发布。

- 更新日志维护在 `CHANGELOG.md`；版本号、Git 标签、Release 和安装包必须对应同一份源码及产物。
- 复用 `installer/Build-Installer.ps1`；正式更新地址和上传凭据未就绪时，明确报告缺失项，不将本地打包称为发布完成。
- 不提交密钥、签名私钥、构建缓存、无关本地配置或安装包；安装包作为 Release 附件分发。
- RustFS 保留旧版本文件，新版本文件先上传并校验，最后更新入口清单；不得修改已签名内容。
- 发布结果需包含提交、标签、Release 链接、下载地址及验证结果；失败时说明已完成步骤和待补步骤。

## Excel 验证

实机调试必须使用 `/x` 新建独立 Excel 进程，只操作确认归属的测试 PID/HWND，不影响用户现有 Excel。未执行的实机验证标记为 `NOT_RUN`。
